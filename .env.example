# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/
OPENROUTER_API_KEY=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

# Model Configuration
# Budget-Recommended (Testing): ~$0.02-0.05 per analysis
PERFORMANCE_ANALYST_MODEL=deepseek/deepseek-chat          # $0.14/$0.28 per 1M tokens
CRITIQUE_AGENT_MODEL=deepseek/deepseek-chat                # $0.14/$0.28 per 1M tokens
SYNTHESIZER_MODEL=deepseek/deepseek-chat                  # $0.14/$0.28 per 1M tokens

# Alternative Model Options:
# Ultra Budget (All DeepSeek): ~$0.01-0.03 per analysis
#   SYNTHESIZER_MODEL=deepseek/deepseek-chat
#
# Better Synthesis (Moderate Cost): ~$0.05-0.15 per analysis
#   SYNTHESIZER_MODEL=anthropic/claude-3-haiku             # $0.25/$1.25 per 1M tokens
#
# Premium Quality (Expensive): ~$0.20-0.50 per analysis
#   SYNTHESIZER_MODEL=anthropic/claude-3.5-sonnet          # $3.00/$15.00 per 1M tokens

# Email Configuration (Optional - for email delivery)
# Note: Reports are always saved locally, email delivery is optional
# For Gmail: Use App Password (not regular password)
#   1. Enable 2FA on your Google account
#   2. Go to: Google Account → Security → App Passwords
#   3. Generate password for "Mail"
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here              # Gmail App Password, NOT regular password
EMAIL_FROM=your_email@gmail.com
EMAIL_TO=recipient@example.com

# ArXiv API Configuration
ARXIV_API_BASE=http://export.arxiv.org/api/query
ARXIV_MAX_RESULTS=10

# Output Configuration
OUTPUT_DIR=outputs/reports
