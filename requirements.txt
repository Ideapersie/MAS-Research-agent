# Core AutoGen and LLM
# Using older stable version (0.1.x) as newer versions (0.7+) have breaking API changes
pyautogen==0.1.14
# openai<1 is required by pyautogen 0.1.14
# For OpenRouter compatibility, we don't need newer openai package

# MCP SDK
mcp>=1.0.0

# ArXiv Integration
feedparser>=6.0.0
requests>=2.31.0

# Email
secure-smtplib>=0.1.1

# PDF Generation
reportlab>=4.0.0
markdown2>=2.4.0

# Configuration
python-dotenv>=1.0.0

# Data Processing
pydantic>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
